let initialState = {
	friends: [
		{name: "Vlad"},
		{name: "Dmitry"},
		{name: "Alexander"},
		{name: "Danil"}
	]
}

const navbarReducer = (state = initialState, action) => {


	return state
}

export default navbarReducer